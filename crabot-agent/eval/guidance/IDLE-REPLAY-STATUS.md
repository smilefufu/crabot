# 两次自省事故回放：已完成真实对照

2026-09-18。用户明确批准事故历史发送至原端点后，真实回放与正向对照已完成。此前自动审批阻断已解除，不再处于“请求为 0”的状态。

- 两处历史各三次：旧版 2/6 错误外发，候选 0/6；十二条均实际查板、无接口缺口、无中断。
- 同一会话连续五次：旧版在第 1/3/5 轮错误外发；候选五轮未外发，但其中三轮未重新查板，不能把沉默或少工具调用直接等同于全部自省职能通过。
- 候选能提出必要的 A/B 问题、正常交付包含失败项的检查结果，也能由自省主动发现“历史已恢复、任务板仍暂停”并真实派工完成。新人类消息触发的恢复样本虽然完成读写与交付，但收尾触及 16 请求预算，保留为未完整结束。
- 初批多注入当前 bot handle 的六条装配检查与花费另列，未混入有效对照；修正后基线首请求输入与两次事故 trace 一致，仍不声称完整 Provider payload 逐字重放。
- 含装配检查共 156 请求、2,685,178 tokens。未发送飞书，未改变产品提示词或部署；此前候选权限反例仍待处理。

详细逐条结果、缓存与性能限制、冻结摘要及证据路径见 [真实自省历史回放报告](IDLE-REPLAY-RESULTS-20260918.md)。评测器的历史恢复、连续 wake、本地投递和 Docker 工具边界已经过 12 项去重回归及真实历史装配检查；脚本模型的本地通过不计入上述真实模型质量成绩。
