# 内置 guidance 隔离比较

**2026-09-17 当前状态：两份重复取证指南的定向调整尚未证明有效。** 用户批准后冻结比较修改前 `cb33163f` 与本地候选，实际 89 次请求、1,424,398 tokens；5 条正常收口、1 条交付后内部收尾被预算截断、2 条未开始。两个正常完成配对 tokens 少 20.6%，但执行器业务调用从 18 增至 21。验收规则见 [EVIDENCE-ACCEPTANCE.md](./EVIDENCE-ACCEPTANCE.md)，具体质量、动作、用量及人工历史边界见 [EVIDENCE-RESULTS.md](./EVIDENCE-RESULTS.md)。入口为 `evidence-compare.mjs`；此批未改变核心、工具或缓存实现。用户随后确认提示词语义定稿，并授权正式 PR、auto review、自动合并后本地部署；该授权不改变比较结论，也不表示整体行为提升已获证明。

此前修复版 `ee4c5c6d` 的完整任务比较：开始 29 条、完整收口 28 条，1 条预算中断、3 条未开始。可完整配对的 13 对均完成实际目标，旧版三条有实质汇报缺陷、新版未出现同类缺陷；新版诊断过程更重，配对总 tokens 多 12.7%。不能宣布整体质量或效率胜出。证据见 [COMPLETION-RESULTS.md](./COMPLETION-RESULTS.md)，更早短决策与撤回批次见 [RESULTS.md](./RESULTS.md)，标准见 [ACCEPTANCE.md](./ACCEPTANCE.md)。各批统计不混用。

此前完整任务入口为 `completion-compare.mjs`；`--prepare` 只冻结人工案例、基线、候选与编译产物摘要，不读凭据或调用模型。执行前先运行 `node --test crabot-agent/eval/guidance/docker-fixtures.test.mjs crabot-agent/eval/guidance/history-runtime.test.mjs crabot-agent/eval/guidance/completion-cases.test.mjs`。原始初批 200 万停止线保留在脚本；补充批次只运行原计划未开始条件，具体冻结与用量调整见结果记录，不覆盖初批目录或重抽中止轨迹。

## 正式 PR 前工程复核（2026-09-17）

- Agent 广泛回归：256 个文件、3356 项，首轮 3336 通过 / 20 失败。两个失败是 builtin 分支仍匹配旧提示词文案；保留原工具执行、历史继承与分支隔离断言，仅更新对应文案，随后该文件 86/86 通过。
- 剩余 18 项在未修改的当前 main `fa645866` 上复现：权限夹具缺必需 Skill / 配置回归与子进程环境守卫共 9 项、macOS 临时目录路径断言 3 项、原生 CLI/tmux 既有用例 6 项。CLI 串行复核还观察到追加监听用例的间歇性失败，两边均出现；不把这些记为通过，也未并入无关修复。
- Admin Skill 注册/迁移本轮 2 文件 45/45 通过；Agent 与 Admin TypeScript 构建通过；两仓 `git diff --check` 通过。
- 当前改动覆盖的角色目录、权限查询、任务板首次不写入、缓存前缀、恢复/压缩清理、内置子 Agent 隔离和 CLI 正文注入测试均已运行。真实 CLI 模型行为、代表性业务分布和全部权限组合不据此宣称全面验证。

## 后续分阶段验收

新的验收标准、十项人工决策案例和用量边界见 [ACCEPTANCE.md](./ACCEPTANCE.md)。第零阶段修复评测器的并发请求取消及 Docker 进程数干扰；两个问题先复现失败，修复后相关 9 项通过。旧结果不修改、不补样。

第一阶段 `decision-compare.mjs` 只观察下一步决策，不执行业务工具，不作为完整任务质量不退步的证明。先运行 `node --test crabot-agent/eval/guidance/decision-runtime.test.mjs`，再用 `GUIDANCE_FROZEN` 指定既有基线包、`GUIDANCE_OUTPUT` 指定新目录，执行 `node crabot-agent/eval/guidance/decision-compare.mjs --prepare`。准备过程不读取 Provider 配置、不联网；实际发送的提示词和工具描述、脚本摘要、案例、预算与判定标准冻结在 `inputs.json`。

得到本轮具体发送范围的确认后，设置与原批次相同的 `REPLAY_RUNTIME_ROOT`、`REPLAY_DATA_DIR`，移除 `--prepare` 启动。只解析唯一匹配的原百炼 Qwen 配置；发生错误停整批，无自动恢复。审阅包和标签映射分别落盘；隐藏版本标签不等于独立盲评，原始输出不上传。

第一阶段旧缺陷批次完成了 40 条短轨迹、55 次请求，分项审阅见 [RESULTS.md](./RESULTS.md)。旧新各有仅需文字时选择多余操作的反例；预告不等于已委托，没有正文也无法评价文档语义。不得把 `decision_observed` 的 40 条全部计作通过。决策测试器 8 项回归通过。

## 历史问题的完整续办比较

`history-cases.mjs` 从可核实的旧记录重建三个失败机制：主控重复请求已有授权、主控继续传播已撤销限制、执行器归档时按扩展名排除必需题集。名称和数据均为人工内容，不向模型发送原始会话或业务文件。第三项是用户要求回看近三天问题时发现的具体执行错误，并非用户逐字点名该命令。历史发生过失败不代表改动前基线必然失败；其间已有修复，不能预设比较结果。

`history-runtime.mjs` 装配产品 Manager、Harness、Builtin Worker、原生 trace 和回合结果。初始阻塞报告是固定历史状态，之后由模型决定续办，实际文件和命令使用隔离 Docker 产品工具。对外消息只写本地 outbox，记忆也是本地边界，不测试真实渠道投递或记忆服务；未实现的边界明确报错。主控比较固定下游 Worker 的候选正文，执行器案例才切换 Worker 正文。双方沿用当前产品代码与工具说明，比较的是提示词/guidance，不是两个完整旧新生产系统。

运行前执行 `node --test crabot-agent/eval/guidance/history-runtime.test.mjs`：三条完整本地链路与一次历史错误负向检查，共 4 项通过。模型为脚本响应，仅用于验证测试装配；业务操作真实执行。人工收件箱、源文件保护、真实回合读取和独立产物检查都不依赖模型自称完成。历史归档漏题集必须被独立检查拒绝。

准备方式与下方 Docker 比较相同，将脚本替换为 `history-compare.mjs`。3 例、新旧各 2 次，共 12 条轨迹；主控轨迹每条最多 24 次请求（含实际执行器），执行器轨迹每条最多 12 次，总上限 240 次。每次等待静止最多 10 分钟，每次模型响应最多 90 秒；等待上限不是整条轨迹的硬截止时间。首次模型异常即中止该轨迹，不自动补样；正反顺序交替。运行日志按请求保留角色、工具、用量和文件快照，不输出模型隐藏推理。

2026-09-17 用户明确批准后已完成：原 12 条轨迹中旧版首条被额度耗尽中断。用户重置额度要求再试后，`history-recover-quota.mjs` 仅恢复这一条：检查原文件未改、只存在两次成功决策及额度错误，重放已收到的决策，映射新 Worker ID，再补完后续。原 4 次尝试加最多 20 次新请求仍不超单条上限；实际新增 15 次。该脚本不是通用恢复入口，不重抽成功决策，旧日志不改。

全批含恢复实际 188 次模型请求、185 个完整响应；主要目标旧版 6 条完成、新版 5 条。新版一次把文件输入固化成“唯一认可”方式且未交付。新版归档第一次遇到容器进程上限，新版撤限第二次遇到主控读容器目录及记忆写入的本地边界；成本比较须保留这些限制。完整结果见 [RESULTS.md](./RESULTS.md)。

## Docker 真实执行比较

`docker-compare.mjs` 使用新旧提示词处理两个独立人工任务：修复加法脚本、只读定位报表路径错误。Read、Edit、Write、Bash 直接执行产品实现；文件和 Python 进程在专用 Docker 容器内运行，支持正常组合命令，不按命令文字匹配模拟回执。guidance 使用产品只读实现。

每版每例一个新容器，无网络、无宿主目录挂载、无业务凭据，以普通用户运行，根文件系统只读，人工工作目录可写。只读案例仍保留写工具，用运行前后的文件内容和权限快照检查模型是否遵守要求。独立验算不信任模型的完成声明；`finish_task` 使用产品工具说明、仅记录收尾报告，不测试 Worker 台账生命周期。

先构建 Agent，再按顺序执行：

```sh
node crabot-agent/eval/guidance/build-docker.mjs
node --test crabot-agent/eval/guidance/docker-fixtures.test.mjs
GUIDANCE_FROZEN="$BASELINE_INPUTS" GUIDANCE_OUTPUT="$NEW_OUTPUT_DIR" \
  node crabot-agent/eval/guidance/docker-compare.mjs --prepare
```

镜像仅装入本次工具代码与公开运行依赖；不会把整个工作区作为构建上下文。离线检查覆盖真实失败→修复→独立验算、引擎拒绝 Edit 后文件不变、真实报错的只读诊断、`task=false` 时产品执行条件查询与派发拒绝一致。派发检查没有调用模型，也没有启动真实 Worker，不能据此声称主控行为已完成端到端验收。

准备包冻结镜像 ID、评测脚本摘要、实际工具说明、基线正文及候选装配。4 条轨迹各最多 8 次请求，共最多 32 次；不重试或补样。外部模型使用下文相同的 Admin 解析方式与固定百炼端点；确认本轮发送范围后，设置 `REPLAY_RUNTIME_ROOT`、`REPLAY_DATA_DIR` 并移除 `--prepare` 运行。连接凭据只用于宿主请求，不进入 Docker。原始请求日志及文件快照留在本机，不提交。

## 历史模拟比较

比较主控、执行侧在 6 个人工案例中的决策。业务工具全部返回固定模拟结果；`compare.mjs` 仅实际调用无副作用的产品 guidance 读取。不会读取真实聊天、业务文件或启动执行器，不执行模型给出的 Shell 命令。

这是有限的决策比较，不是生产任务验收。`response-ended` / `decision-observed` 只表示轨迹停止；必须检查输出、工具调用和证据，不能作为成功计数。`harness_gap`、请求失败和轮次上限分别保留，不自动重试或补样。案例只有每版一次，不能据此推断稳定性或泛化提升。

## 准备与运行

先构建 Agent。`--prepare` 只生成请求包，不读取运行配置、不连接模型。真实运行会使用现有 Admin 连接解析器，限定当前已核对的百炼端点和 Qwen3.8-max；密钥只进入请求头，不写入产物。

1. `preflight.mjs --prepare`：设置 `GUIDANCE_BASELINE_REF` 为改动前 commit、`GUIDANCE_SPEC` 为确认稿、`GUIDANCE_OUTPUT` 为新目录。旧正文通过 `git show` 固定在指定版本，不依赖当前工作区。首轮上限为 12 条轨迹、48 次请求。
2. `compare.mjs --prepare`：设置 `GUIDANCE_FROZEN` 指向首轮 `inputs.json`、`GUIDANCE_OUTPUT` 为另一个新目录。基线沿用首轮，候选使用当前编译后的实际提示词及工具说明。双方均使用同一组工具说明与场景，候选另有 guidance；第二轮上限为 12 条轨迹、72 次请求。
3. 先核对 `inputs.json`、接收方、模型及发送范围。得到这次外部比较的具体授权后，保留上述变量，并设置 `REPLAY_RUNTIME_ROOT` 为已构建 Admin 所在仓库、`REPLAY_DATA_DIR` 为对应数据目录，移除 `--prepare` 运行同一脚本。

输出目录含 `inputs.json` 和逐请求 `events.jsonl`。输入有 SHA-256 摘要；再次使用准备目录必须逐字一致，已有运行日志则拒绝复跑，避免覆盖失败证据。产物默认仅本用户可读，勿将真实配置或凭据加入比较包。

第二轮比首轮补充真实消息投递说明及模拟复核、测试脚本读取与已观察的目录查询，并明确去重任务口径。因此两轮结果不能混合计分。仍未覆盖生产调度、真实投递、文件/Shell 执行及原生 CLI 子 Agent。

## 2026-09-17 首轮记录

基线 `739b50e0`；输入摘要 `662afd507cd02be68664a9e580704250ae33093f4c197aae3e13601d544f133a`。6 组双版本、12 条轨迹，实际 19 次请求、19 次响应，Provider 报告合计 37,763 tokens。

| 案例 | 观察与限制 |
| --- | --- |
| 简单翻译 | 基线调用消息工具，候选只返回内部文字；夹具缺少真实投递说明及生产复核，尚不能判定实际装配回归。 |
| 禁止派发 | 两版均不派发；候选额外建议为主控挂文件工具，并提出过多整理口径问题，未完全满足预期。 |
| 已授权桌面只读 | 两版均派发只读任务，没有执行真实桌面操作；委托正文均冗长并追加若干交付要求。 |
| 空闲但有错误 | 两版均先查错误再说明权限阻塞；候选重复读取已提供的指南。没有把 idle 当作完成。 |
| 简单脚本修复 | 两版均读取目标文件，随后读取测试脚本遇到夹具缺口；未验证修复或交付。 |
| 原因不明的失败 | 两版读取错误日志后请求目录查询，夹具未覆盖；未完成诊断轨迹。 |

4 条执行侧轨迹为 `harness_gap`；其余只观察到决策。当前不能宣布行为验收通过或新版优于基线。

第二轮在用户明确授权后完成 33 次请求，输入摘要未变；完整结果与局限见 [RESULTS.md](./RESULTS.md)。未将模拟器缺口记为模型失败，未宣称整体改善。

`resume-fixture-gaps.mjs` 仅供本次 4 条既有 Worker 轨迹的缺口接续，保留原消息和已返回回执，每条总轮次仍不超过冻结上限，总请求不超过 72；只允许接续一次，不生成新样本。它不适用于生产任务恢复。执行前仍需具体的外部比较授权。模拟器回归运行：`node --test crabot-agent/eval/guidance/fixtures.test.mjs`。
