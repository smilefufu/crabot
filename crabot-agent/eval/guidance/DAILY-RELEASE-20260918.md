# 每日反思与等待、搜索提示词发布核验

2026-09-18。用户在两个任务完成需求交叉核对后，明确要求直接提交推送 main 并更新本地部署。本记录区分发布检查与模型行为验收，不以部署或测试通过代替实际反思完成。

## 已确认范围与修改依据

- 依据已发布文档仓 `96c5719` 的每日反思取证与完成 spec、Agent §4.3.1，以及既有 6/18 图谱和 8/20 记忆生命周期设计。
- 应用此前等待、搜索候选：主控与内置执行器只剩等待时结束回合；反馈 guidance 和活动工具避免游标/空结果诱发重复查询；搜索说明明确下一轮可见、权限和预算省略边界；移除记忆搜索指向已退役任务工具的教程。
- 每日反思专用正文、Admin 定时任务指令和收尾提醒共同对齐固定周期取证、分页、等待续办、独立 finish、partial 和摘要回执。新复盘结论带来源进入 inbox 并本轮核验；增量建链默认不连，保留旧边，读取失败不猜测覆盖。
- 完成工具立即退出本回合，宿主随后校验与确认水位；不将退出标记当成功，不要求模型在退出后读取工具回包。合法续办从 previous_result 的 pending_items / validation_errors 核对结果。
- 不增加权限、工具参数、宿主完成门槛、失败台账、同参数重试要求或 Skill。set_memory_links 仅补明既有替换语义，未改变实现。详细参数留在工具说明。

## 定向验证

- Agent：12 个测试文件、242 项定向用例通过（包含最终重跑的 41 项 tool-face 用例）。覆盖独立角色装配、guidance、权限、Memory 工具面、取证分页、反思完成与续办、Manager loop 和 trace 投影。
- Admin：2 个测试文件、23 项用例通过，覆盖内置任务首次注册/更新及身份、运行统计保留，完成确认和水位保护；在最终 Shared 依赖组合下重跑通过。
- Shared、Agent、Admin TypeScript 构建通过。构建前为 worktree 单独构建 Shared，避免借用线上旧声明产物。
- 实际编译装配中，每日反思只使用独立正文；主控核心 300、内置执行器核心 198、每日反思正文 1714 个非空白字符。长度不是请求 token 或行为改善的证据。
- 常驻工具清单仍满足原 20 KiB 上限。首轮说明超预算，已精简本次修改的说明后通过，未放宽测试阈值。两处原 Manager 称呼断言和一处旧核心措辞断言同步当前文案。
- 通读全部改动；仅涉及提示词/说明、对应旧文案断言和本发布记录，无新增执行分支、进程、超时或持久化机制。无需额外代码简化。

## 部署检查与行为验收边界

部署需从已推送 main 依次构建 Shared、Agent、Admin，定向重启 Admin 与 Agent，再核对健康状态、进程路径、新工具面、编译正文及内置任务描述。保留 schedule 身份、计数与历史水位，不因上线而触发补审或跳过历史。部署结果在本次对话及本地核验记录中报告。

本轮未新增 Provider 回放、未主动触发真实每日反思。因此不能据此宣称模型行为全面改善或实际反思已完成。后续实际运行仍需核对新发现和旧 inbox 的处理结果、增量旧边保留、错误纠正/未完成如实报告、等待恢复、摘要与独立 finish，以及最终 outcome 和水位。既有历史缺口不自动重设。
