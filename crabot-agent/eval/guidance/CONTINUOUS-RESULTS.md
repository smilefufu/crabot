# 完整执行对照结果

2026-09-18。状态：首批达到停止线，原计划未启动项待接续；尚未形成最终比较结论。

用户已明确授权本 session 此类验证。实际发送范围为事先冻结的 12 类人工案例、34 条轨迹，百炼 qwen3.8-max；不加入私人历史、业务文件或真实渠道投递。产品基线 afc49624，候选产品正文与装配仍为 ea1103f1。

冻结输入 SHA-256：a258e341b9667970baa4026e8a1fe0bf7a030b503d709a77ae1009329c960922。原始记录目录：`/private/tmp/crabot-guidance-continuous-20260918`。

## 评测器验证

21 项 Docker/脚本模型检查通过，包括查板后继续到外发、指南与业务查询同批后继续推理、真实权限查询、本地发送、虚假完成被独立产物检查拒绝、项目已有改动保留、真实主控派工与修复提交闭环、独立内置 reviewer 的结果回传。它们验证评测器机制，不计入真实模型成功率。

准备中发现并修正：权限工具可见性不等于执行授权，测试应核验真实 can_spawn；容器目录、Git 观察及规则快照须统一使用容器文件系统。这些调整在冻结和实际模型请求之前完成，原失败日志保留。

## 限制

人工案例保留事故结构，但不是未留存的原始 Provider 请求的精确重放。当前批次没有三方原生 Worker、真实 Feishu 投递、计时器等待或独立盲评。模型语义结论须审查完整轨迹；程序结束和产物存在均不能单独证明任务完成。

## 首批停止线

实际 118 个模型请求、118 个完整响应，2,011,190 报告 tokens（停止线之后已准入响应正常收完）。14 条完整收口；manager-project/1/candidate 在提交前因全局预算中断，独立产物检查未通过；19 条未启动。中断轨迹与未启动项分开，不当作模型质量失败或完成。

当前已见新版权限消息多要了已有 file_io/shell 权限；旧版将单次采购约定泛化写入长期记忆。旧版一条 get_sessions 和两条管理端拓扑查询遇到未接通的本地传输，重复提醒例的身份解析也发生回退。受影响项不作干净的性能配对证据；所有原始错误保留。

原计划未启动项将另设 200 万 tokens 停止线接续；不重抽首批已完成或中断轨迹。
