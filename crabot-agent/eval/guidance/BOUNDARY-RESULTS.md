# 核心规范与工作流归位：验证记录

日期：2026-09-18。旧版基线为主仓 afc49624；本次在独立分支 fix/guidance-workflow-boundaries 验证。尚无真实模型结果，不宣称新版行为更优。

## 本地验证

| 验证 | 实际结果 |
| --- | --- |
| 先复现再修复 | 旧源码 5 项失败：普通 full/progressive 首次改板被指南拦截、真实错误被前置成功回执掩盖、管理员改板在独立及运行中场景误加载自省 |
| 定向产品回归 | 16 文件共 442 项通过，重复运行的 16 项 guidance 检查不重复计数 |
| Docker 真实工具链 | 17 项通过；容器无网络、无宿主挂载，文件、进程、产物检查使用真实实现，模型响应由脚本提供 |
| 评测装置 | 12 项通过，其中本次 3 项覆盖真实装配、业务选择不执行、指南进入尾部及接口失败不重试 |
| TypeScript | Agent 构建通过 |
| 正文一致性 | 两个核心及七份正文与增量设计逐字一致；核心长度 298/191 |

定向范围含任务板 Store/read fence、工具面及首次写入、管理员更新、自省触发、Manager bootstrap、运行中注入、恢复、压缩/失败清理、固定 system 与缓存键、builtin 生产装配/适配器、CLI guidance stdio、内置子 Agent 提示词隔离。未改每日反思或三方原生子 Agent 继承。

首轮扩大回归有两条测试断言失败：一次把自省恢复的临时消息数也误改为管理员通知数量，一次仍匹配旧版提交措辞。已修正测试预期后通过；未据失败修改产品行为或删除验证。

## 模型验证准备与当前限制

- 18 个纯人工案例：自省暂停、健康执行、需要人类决定、重复阻塞、阻塞解除；管理员改板、普通建项、简单答复；内部/部分/最终 Worker 结果；权限不足；复杂委托；项目接手；Worker 项目、协作、诊断及一次性读取。
- 旧新各一次，交替先后，共 36 条轨迹；每条最多 4 请求，总上限 144 请求、已报告用量 50 万 tokens。只使用百炼固定端点及 qwen3.8-max，不发送私人历史、业务文件或凭据，不执行业务动作，不加模型裁判、不重抽。
- 最终冻结 SHA-256：`dd44460d1ab1324153b44c132aeea47109b21baf37d79133ea48dff2efd79783`。首个准备包未执行；最终包只恢复诊断第 5 步的“修复后”条件，避免只诊断场景误入修复验证。
- 自动审批在实际调用前拒绝，理由是用户对测试的授权尚未明确覆盖本次具体提示词与百炼目的地；已发起具体范围确认。**目前外部模型请求数为 0，无模型行为或 token 对比结果。**
- 本地测试证明装配和执行边界；不能证明模型绝不外发自省内容，也不能证明整体任务质量、缓存命中率或成本改善。真实模型决策验证仍待授权与执行；即使通过也只构成这些边界案例的有限证据。

## 本次审查

七份内容已逐段核对：任务板职责及记录归属进核心；自省保留严格外发条件和先必要性后去重；反馈保留必要验证、有效证据复用及最终交付正向分支；项目保留目录、基线、授权、文档和主线提交边界；协作不覆盖子角色；诊断不以省调用替代必要验证。运行修改仅删除旧任务板指南前置状态与管理员误触发，不增加持久状态、权限或投递拦截器。

本次增量未推送、未建 PR、未部署。
